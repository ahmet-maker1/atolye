{
  "name": "atolye-smartphone-system",
  "version": "1.2.0",
  "private": true,
  "description": "Bıyıklı - Gsm — İkinci el telefon envanter & satış yönetim sistemi (Node + SQLite + React)",
  "scripts": {
    "install-all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "seed": "npm --prefix server run seed",
    "reset-db": "npm --prefix server run reset"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
